/* ACCESO: valida credenciales y redirige según el rol del usuario. */
document.querySelector('#login-form').addEventListener('submit', async (event) => { event.preventDefault(); const response = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); const data = await response.json(); if (!response.ok) { document.querySelector('#login-message').textContent = data.message; return; } location.href = data.user.role === 'administracion' ? '/admin' : data.user.role === 'ventas' ? '/ventas' : '/cuenta'; });
